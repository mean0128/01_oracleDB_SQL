1, jdk 설치
  https://www.oracle.com/kr/java/technologies/javase/javase8-archive-downloads.html
  jdk-8u151-windows-x64 다운로드
  default 설치

2, oracle Express Edition 11g 설치
  https://developer.oracle.com/databases/
  11.2.0.1.0버전으로 default설치

3, DB실행 및 설정 : 추후 유저명과 비밀번호가 필요한경우, 유저명=scott, 비밀번호=tiger
  Run SQL Command line 실행
  
  관리자 접속
    conn /as sysdba
  create user 유저명 identified by 비밀번호
  default tablespace users
  temporary tablespace temp;
  
  유저에 권한 부여
    grant connect, resource, plustrace, alter session to 유저명;
  
  유저로 접속
    conn 유저명/비밀번호
  
  *, 유저 잠금 해제
    conn /as sysdba 또는 conn system/1234(초기 설치시 세팅하게 되어있음)
    alter user 유저명 account unlock;
  
  *, 유저 암호 변경
    alter user 유저명 identified by 변경할 암호

4, Eclipse 설치 및 설정
    https://www.eclipse.org/downloads/packages/release/oxygen/2/eclipse-ide-java-ee-developers
    eclipse-jee-oxygen-2-win32-x86_64.zip 압축 해제(c:\\에 추천)
    압축 해제된 폴더로 가서 eclipse 바탕화면에 바로가기 만들기 및 실행
    
    *, 최초실행시
    작업폴더 C:\\workspace\\SQL 추천
    Use this as the default and do not ask again 체크
    처음뜨는 화면 닫
    

  DB설정
    우측 상단 Open Perspective → Database Development → Data Source Explorer → Database Connections 우클릭 → 
    NeW → Oracle 선택 → Drivers 우측 New Driver Definition 작은 아이콘 선택 → Name/Type 에서 Oracle Thin Driver 11 →
    JAR List에서 Add c:/oracleexe/app/oracle/product/11.2.O/server/jdbc/lib/ojdbc6.jar 선택 →
    Service Name : xe, Host : locaHost, Username : 유저명 PassWord : 비밀번호, save password 체크

  Project 생성
    우측 상단 JavaEE → Project Explorer → NeW → Other → Web : Dynamic Web Project → Name : 작업할 폴더명

  폴더 및 파일 생성 
    우측 상단 JavaEE → Project Explorer → Web Content폴더 우클릭 → NEW : Folder → 폴더명 입력 → 생성된 폴더 우클릭 →
    NEW : SQL File → 파일명 입력 → 파일 더블클릭 → 상단 Connection Profile : Type=Oracle 11, Name=NEW Oracle, Databasee=xe
  
  인코딩 설정
    window → Preferences → General → workspace → Other: UTF-8
  
  *, DB 연결안된 경우 해결방법
    우측 상단 Database Development 클릭 → New Oracle 우클릭 → Connect
  
5, SQL developer 설치 : 테이블 시각화, 엑셀 load & Save
  sqldeveloper-x64 압축해제 → 실행 → 업데이트 안함 → 좌측상단 "+" 아이콘 선택 → 
  접속 이름 : 임의로 설정 , 사용자 이름 : scott 비밀번호 : tiger
  
*, 작성한 코드 실행 
  Alt+s

*, exerd 설치
  help → Install New Software → Work with : http://exerd.com/update → name exerd →
  File → New → Other → eXERD : 리버스 엔지니어링
